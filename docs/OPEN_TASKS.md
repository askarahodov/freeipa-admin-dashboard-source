# Открытые задачи

Актуально на 28 июля 2026 года.

## Следующий приоритет

### P0 — эксплуатационная приёмка

- [x] Автоматизировать проверку bootstrap-admin, cookie policy, блокировки входа, разблокировки и персистентности после перезапуска (`npm run test:p0:acceptance`).
- [ ] Прогнать локальный acceptance-сценарий на реальных тестовых FreeIPA и XYOps.
- [ ] Проверить bootstrap-администратора на полностью чистой базе.
- [ ] Проверить через браузер роли `viewer`, `operator` и `admin` в локальном окружении пользователя.
- [ ] Проверить сохранение пользователей, ролей и настроек после перезапуска Docker Compose.
- [ ] Проверить блокировку входа, смену пароля и отзыв сессий.
- [ ] Сохранить результаты проверки и оформить обнаруженные дефекты.

Автоматизированная часть описана в `docs/P0_OPERATIONAL_ACCEPTANCE.md`. Пункты реальной приёмки остаются открытыми до запуска в локальном контуре с тестовыми интеграциями.

### P1 — E2E

- [x] Playwright: вход, выход и неверный пароль (`npm run test:e2e:auth`).
- [x] Playwright: создание локального пользователя и назначение роли.
- [x] Playwright: ограничения ролей `viewer`, `operator` и `admin`.
- [x] Playwright: FreeIPA CRUD и управление membership (PR #24).
- [x] Playwright: XYOps run, approval, cancel и result (PR #25).
- [ ] Playwright: сохранение состояния после перезапуска.

Браузерные сценарии выполняются в отдельном Docker-контуре и подтверждаются workflow `Auth E2E` с реальным Chromium.

## Переработка административного интерфейса

### P1 — упрощение административного доступа

- [ ] #26 — заменить повторный `ADMIN_TOKEN` в браузере на локальную admin-сессию; service token оставить для внешнего API.
- [ ] #27 — разделить настройки на самостоятельные административные страницы и компоненты.

### P1 — каталог и автоматизация XYOps

- [ ] #28 — удалить legacy automation routes и заменить их ярлыками/пресетами процессов XYOps.
- [ ] #29 — сделать визуальный центр совместимости и проверки реальной схемы XYOps.

### P1 — визуальные редакторы политик

- [ ] #30 — заменить JSON видимости каталога визуальным rule builder и симулятором доступа.
- [ ] #31 — заменить JSON approval policy визуальным редактором согласований.
- [ ] #32 — сделать интерфейс многоязычного представления процессов.
- [ ] #33 — добавить полный Playwright E2E переработанного административного интерфейса.

Серверные visibility, approval, `schemaVersion`, audit и encryption-контракты сохраняются. Перерабатывается пользовательский интерфейс и устаревшая модель automation routes.

### P1 — диагностика

- [ ] Полный диагностический архив с локальными логами и обязательным redaction секретов.
- [ ] Проверка архива тестами на отсутствие паролей, API keys, токенов и session cookies.
- [ ] Кнопка формирования и скачивания архива только для администратора.

## Следующий функциональный цикл

### P2 — дополнительные модули FreeIPA

- [ ] Hosts и host groups.
- [ ] Services и service principals.
- [ ] HBAC rules.
- [ ] Sudo rules.
- [ ] DNS records и zones.
- [ ] Сертификаты.
- [ ] Automember rules.

Перед реализацией каждого модуля требуется отдельно определить FreeIPA JSON-RPC методы, необходимые права service account, подтверждения опасных действий и аудит.

## Отложено

### Мониторинг и эксплуатационные метрики

Задача сохранена, но не является текущим приоритетом.

- [ ] Отдельные `/health`, `/ready` и `/metrics` endpoints.
- [ ] Prometheus-метрики портала, SQLite/D1, FreeIPA Gateway и интеграции XYOps.
- [ ] Сбор метрик Prometheus или совместимым агентом.
- [ ] Grafana dashboard для состояния портала и интеграций.
- [ ] Alerting по недоступности портала, FreeIPA Gateway и XYOps.
- [ ] Документация по подключению к существующему стеку мониторинга.

Причина переноса: сначала необходимо завершить реальную локальную приёмку, переработку административного интерфейса и диагностический архив.
