name: Apply catalog visibility policies

on:
  push:
    branches: [agent/security-hardening]

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/security-hardening
          show-progress: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm

      - name: Apply catalog policy patch
        run: python scripts/apply-catalog-policies-patch.py

      - name: Verify implementation
        id: verify
        shell: bash
        run: |
          set +e
          {
            npm ci &&
            npm run lint &&
            npm test
          } 2>&1 | tee /tmp/catalog-policies-ci.log
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Commit result
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ "${{ steps.verify.outputs.status }}" = "0" ]; then
            rm -f scripts/apply-catalog-policies-patch.py
            rm -f .github/workflows/apply-catalog-policies.yml
            rm -f .github/catalog-policies-ci-failure.txt
            git add -A
            git commit -m "Add user and group catalog visibility policies"
          else
            git reset --hard HEAD
            tail -n 500 /tmp/catalog-policies-ci.log > .github/catalog-policies-ci-failure.txt
            git add .github/catalog-policies-ci-failure.txt
            git commit -m "Record catalog visibility policy CI failure"
          fi
          git push origin HEAD:agent/security-hardening
