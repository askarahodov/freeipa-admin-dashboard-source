--- a/worker/README.md
+++ b/worker/README.md
@@ -20,7 +20,7 @@
 
 Local security ordering is deliberately **not** represented as one universal authentication-before-origin pipeline. `middleware/local-security-routing.ts` now owns the ordinary local-session/service-admin-fallback/origin routing at the historical adapter position, while `local-secure-entry.ts` retains `/api/auth/**` handling. The route-specific semantics remain: local `/api/auth/**` mutations validate same-origin before administrator session authorization (and logout does not require `requireAdmin`), while an already authenticated local administrator reaching an integration path resolves the session before same-origin validation and internal-token delegation. A missing local session reaches the allowlisted service-admin fallback before ordinary API denial. `security-composition-contract.ts` records these local order profiles explicitly so later middleware extraction cannot silently change 401/403 behavior.
 
-Phase #631 checkpoint A consolidated the former `freeipa-group-member-entry.ts -> freeipa-user-bulk-entry.ts -> freeipa-user-query-entry.ts` wrapper chain into one `freeipa-http-entry.ts` compatibility owner at the same historical position. Checkpoint B1 extends that same HTTP owner to the base users/groups reads. `freeipa-base-read.ts` now contains only route-neutral directory projection/fallback logic, `freeipa-rpc.ts` owns the reusable Gateway/direct transport, and `integration-settings-runtime.ts` preserves the existing persisted-settings-to-environment resolution and encrypted-secret format used by both the adapter and the central settings runtime. Query/filter/pagination, CSV export, bulk orchestration, group-member projection and the legacy status-permission projection keep the same downstream auth/permission behavior. `POST /api/integrations/freeipa/actions` deliberately remains in `worker/index.ts` for B2 because its audit and operation-run persistence are shared with the central integration runtime.
+Phase #631 consolidated the former FreeIPA wrapper chain into one `freeipa-http-entry.ts` HTTP owner. Checkpoint B1 moved the base users/groups reads into that owner; B2 routed `POST /api/integrations/freeipa/actions` through the same adapter **after** `secure-entry.ts` identity normalization and made bulk reuse the same local mutation handler. `freeipa-base-read.ts` contains route-neutral directory projection/fallback logic, `freeipa-action-runtime.ts` contains route-neutral mutation normalization, `freeipa-rpc.ts` owns the reusable Gateway/direct transport, `integration-settings-runtime.ts` preserves persisted FreeIPA settings precedence, and the shared `portal-access-runtime.ts` / `operation-run-runtime.ts` helpers preserve access and run-history semantics without duplicating them in `worker/index.ts`. The central Worker no longer owns a FreeIPA HTTP route; unmatched traffic still falls through from the adapter to the existing integration/Vinext runtime.
 
 Keeping the metadata contract separate from the runtime seam remains intentional: architecture/parity tests can execute the contract without loading the entire legacy Worker graph. Each following gate may move into explicit middleware only together with focused parity/negative evidence; do not create a second principal/context, default-admin path, or universal service-token bypass while migrating it.
 
